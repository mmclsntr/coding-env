version: "3.0"

services:
  workspace:
    build: .
    restart: always
    tty: true
    volumes:
      - ./app:/root/app
      - ./settings:/root/settings
    environment:
      - 'PS1=[\u@\h \W] \$$ '
    command: /bin/bash -c "eval \"$(anyenv init -)\" && bash"
